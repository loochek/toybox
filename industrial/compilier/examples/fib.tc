fn fib(num)
{
    if (num - 1)
    {
        if (num - 2)
            return fib(num - 1) + fib(num - 2);
        
        return 1;
    }
    
    return 1;
}

fn main()
{
    let x = print(fib(input()));
}
