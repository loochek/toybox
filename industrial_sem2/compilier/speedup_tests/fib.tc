fn fib(num)
{
    if (num <= 2)
        return 1;
    
    return fib(num - 1) + fib(num - 2);
}

fn main()
{
    let n = 30;

    let i = 0;
    while (i < 20)
    {
        fib(n);
        //print(1);
        i = i + 1;
    }
}
